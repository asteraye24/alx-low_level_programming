#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: integer param1
 * @b: integer param2
 * Return: success and integer
 */

int mul(int a, int b);
{
	int a;
	int b;

	printf("%d * %d: %d\n", a, b, mul(a, b));
	return (0);
}
